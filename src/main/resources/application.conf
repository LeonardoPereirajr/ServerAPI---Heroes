ktor {
        development = true
        deployment {
            port = 8080
            port = ${?PORT}
            watch = [ classe, resources ]
        }
        application {
            modules = [ com.project.ApplicationKt.module ]
        }
}